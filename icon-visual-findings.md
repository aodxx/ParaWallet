# ParaWallet icon verification

The normal 512x512 PNG icon preserves the existing green rounded-square brand mark with a centered pale-leaf symbol and clean white accent stroke. The maskable 512x512 PNG uses a full green background and scales the mark inward with generous safe-area padding, so adaptive Android launcher masks should not clip the symbol. Both files rendered at the expected dimensions with no visible edge artifacts.
